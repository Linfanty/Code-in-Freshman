#include<stdio.h>

#define MA 10000+10;
#define MAXB[MA];
#define MAX[MA];
main()
{int n;
memset(MAXB,0,sizeof(MAXB));
memset(MAX,0,sizeof(MAX));
scanf("%d",&n);
for(MAXB=1234;MAXB<=10000;MAXB++)
{MAX=MAXB*n;
printf("%d%/%d"="%d",MAX,MAXB,n);
                                    }
for(;;)
;      }
